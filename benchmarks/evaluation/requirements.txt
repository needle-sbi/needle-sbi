# Requirements for PseudoModel Benchmark Analysis Framework

# Core dependencies (likely already installed with NEEDLE)
torch>=2.0.0
numpy>=1.24.0
pyyaml>=6.0

# Plotting and visualization
matplotlib>=3.7.0
seaborn>=0.12.0
pandas>=2.0.0

# Optional: for interactive plotting
ipython>=8.0.0

# Optional: for notebook-based analysis
jupyter>=1.0.0
notebook>=6.5.0
