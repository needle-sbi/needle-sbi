"""tasks: Backend-agnostic Luigi base layer for NEEDLE task DAG.

Provides shared base classes, mixins, and target utilities that both the
``law_tasks`` and ``b2luigi_tasks`` backends inherit from.
"""
